import React from 'react';

const Footer = () => {
  return (
    <footer className="footer">
      &copy; {new Date().getFullYear()} Amazon Clone — Desenvolvido por Você
    </footer>
  );
};

export default Footer;